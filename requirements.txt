python-telegram-bot>=21.0,<22.0
psycopg[binary,pool]>=3.1.0,<4.0
imagehash>=4.3.0,<5.0
pillow>=10.0.0,<11.0
rapidfuzz>=3.0.0,<4.0
python-dotenv>=1.0.0,<2.0
confusable_homoglyphs>=3.2.0,<4.0
pyrogram>=2.0.0,<3.0
tgcrypto>=1.2.0,<2.0
# Required for time-bounded matching of admin-supplied regex keywords. stdlib
# `re` has no timeout, and it holds the GIL while backtracking, so one bad
# pattern stalls the entire process — see describe_unsafe_regex in
# src/utils/detector.py. Was previously an undeclared transitive dependency.
regex>=2024.0.0
