# Development and CI tooling. Kept separate from requirements.txt so the runtime
# image does not carry a test framework.
#
# Pinned, unlike requirements.txt's ranges: a linter that changes its rule set
# between runs turns CI red on code that did not change.
pytest==8.3.4
ruff==0.15.16
mypy==1.19.1
# Parsing docker-compose.yml and the CI workflow in tests.
PyYAML==6.0.3
